# Copy to .env and change every password before running.
#   cp .env.example .env
# .env is git-ignored. Never commit real passwords.

POSTGRES_USER=hermes
POSTGRES_PASSWORD=change_me_to_something_long
POSTGRES_DB=hermes
