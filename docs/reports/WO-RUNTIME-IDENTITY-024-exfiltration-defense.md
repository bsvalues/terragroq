# WO-RUNTIME-IDENTITY-024 - Secret and Exfiltration Defense

Child environments remove credential variable names. Workspace defense refuses
user-profile traversal and auth/key/browser/SSH/environment material. Patch and
audit scans are value-independent. A suspected leak is terminal and requires
disable/revocation evidence. Outbound executables are limited to Codex, GitHub,
Git, npm, and declared validation tooling; no generic network adapter exists.

Result: `PASS_EXFILTRATION_BOUNDARY`.
