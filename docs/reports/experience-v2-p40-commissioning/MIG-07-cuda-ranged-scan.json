{
  "invocation": "brokeredExec",
  "broker": "lib/fabric/broker.mjs",
  "node": "hermes",
  "action": "mig-cuda-ranged-scan",
  "requireAudit": false,
  "command": "$ErrorActionPreference = 'Continue'\n# #997 MIGRATION — MIG-07 (retry, ranged). Same question as MIG-07: is there ANY published native\n# Ollama whose CUDA runner driver 560.94 can load? The first attempt died at the 540 s broker timeout\n# because it downloaded whole 2 GB archives. This one reads three small byte ranges per release\n# instead, so the sweep can cover a year of releases instead of five.\n\n$tmp = 'D:\\HermesServices\\ollama\\_probe'\nRemove-Item -Recurse -Force $tmp -ErrorAction SilentlyContinue\nNew-Item -ItemType Directory -Force -Path $tmp | Out-Null\n\n$mjs = @'\n// #997 MIGRATION — ranged zip probe.\n//\n// Downloading nine 2 GB release archives to read one DLL's version resource timed the previous\n// attempt out at 540 s. A zip's central directory is at the END of the file and every entry records\n// its own offset, so the same question can be answered with three small Range requests per release:\n// the tail, the central directory, and the ~700 KB of one compressed member. That turns a ~60 s\n// probe into a ~2 s one, which is what makes scanning the whole history affordable.\n//\n// Everything here is read-only against GitHub's CDN. The only local write is the single DLL whose\n// version resource PowerShell then reads, into a scratch path the caller deletes.\nimport fs from \"node:fs\"\nimport zlib from \"node:zlib\"\n\nconst OUT = process.argv[2]\nconst tags = process.argv.slice(3)\n\nasync function range(url, from, to) {\n  const res = await fetch(url, { headers: { Range: `bytes=${from}-${to}` } })\n  if (!res.ok && res.status !== 206) throw new Error(`range ${from}-${to} -> ${res.status}`)\n  return Buffer.from(await res.arrayBuffer())\n}\n\nasync function probe(tag) {\n  const url = `https://github.com/ollama/ollama/releases/download/${tag}/ollama-windows-amd64.zip`\n  const head = await fetch(url, { method: \"HEAD\", redirect: \"follow\" })\n  if (!head.ok) return { tag, error: `HEAD ${head.status}` }\n  const size = Number(head.headers.get(\"content-length\"))\n  const final = head.url\n\n  // End-of-central-directory lives in the last 64 KB at most (it is followed only by the comment).\n  const tail = await range(final, Math.max(0, size - 65_557), size - 1)\n  const eocd = tail.lastIndexOf(Buffer.from([0x50, 0x4b, 0x05, 0x06]))\n  if (eocd < 0) return { tag, size, error: \"no EOCD\" }\n  const cdSize = tail.readUInt32LE(eocd + 12)\n  const cdOff = tail.readUInt32LE(eocd + 16)\n  if (cdOff === 0xffffffff) return { tag, size, error: \"zip64 (not handled)\" }\n\n  const cd = await range(final, cdOff, cdOff + cdSize - 1)\n  const entries = []\n  let p = 0\n  while (p + 46 <= cd.length && cd.readUInt32LE(p) === 0x02014b50) {\n    const method = cd.readUInt16LE(p + 10)\n    const compSize = cd.readUInt32LE(p + 20)\n    const nameLen = cd.readUInt16LE(p + 28)\n    const extraLen = cd.readUInt16LE(p + 30)\n    const cmtLen = cd.readUInt16LE(p + 32)\n    const lho = cd.readUInt32LE(p + 42)\n    const name = cd.toString(\"utf8\", p + 46, p + 46 + nameLen)\n    entries.push({ name, method, compSize, lho })\n    p += 46 + nameLen + extraLen + cmtLen\n  }\n\n  const runnerDirs = [...new Set(entries.map((e) => /^lib\\/ollama\\/([^/]+)\\//.exec(e.name)?.[1]).filter(Boolean))].sort()\n  const dll = entries.find((e) => /cudart64_\\d+\\.dll$/i.test(e.name))\n  const out = { tag, size, runnerDirs, cudartEntry: dll?.name ?? null, entryCount: entries.length }\n  if (dll) {\n    const lh = await range(final, dll.lho, dll.lho + 29)\n    const dataStart = dll.lho + 30 + lh.readUInt16LE(26) + lh.readUInt16LE(28)\n    const raw = await range(final, dataStart, dataStart + dll.compSize - 1)\n    const bytes = dll.method === 0 ? raw : zlib.inflateRawSync(raw)\n    fs.writeFileSync(`${OUT}\\\\${tag}.cudart.dll`, bytes)\n    out.dllBytes = bytes.length\n  }\n  return out\n}\n\nconst results = []\nfor (const tag of tags) {\n  try {\n    results.push(await probe(tag))\n  } catch (error) {\n    results.push({ tag, error: String(error.message) })\n  }\n}\nfs.writeFileSync(`${OUT}\\\\zipprobe.json`, JSON.stringify(results, null, 2))\nfor (const r of results) {\n  console.log(`TAG ${r.tag} size=${r.size ?? \"-\"} dirs=${(r.runnerDirs ?? []).join(\",\")} cudart=${r.cudartEntry ?? \"-\"} err=${r.error ?? \"-\"}`)\n}\n'@\nSet-Content -Path \"$tmp\\zipprobe.mjs\" -Value $mjs -Encoding UTF8\n\n$tags = @('v0.18.0', 'v0.17.0', 'v0.16.0', 'v0.15.0', 'v0.14.0', 'v0.13.0', 'v0.12.0', 'v0.11.0',\n    'v0.10.0', 'v0.9.0', 'v0.8.0', 'v0.7.0', 'v0.6.0', 'v0.5.0', 'v0.4.0', 'v0.3.0')\nWrite-Output '--- RANGED-PROBE ---'\n& node \"$tmp\\zipprobe.mjs\" $tmp @tags 2>&1\nWrite-Output \"NODE_RC=$LASTEXITCODE\"\n\nWrite-Output '--- CUDART-VERSIONS ---'\n# The shipped CUDA runtime's file version IS the toolkit version the runner was built with. Driver\n# 560.94 tops out at CUDA 12.6, so anything reporting 12080 (12.8) cannot load here -- that is the\n# exact failure v0.32.15 hit.\nGet-ChildItem $tmp -Filter '*.cudart.dll' -ErrorAction SilentlyContinue | Sort-Object Name | ForEach-Object {\n    $v = $_.VersionInfo\n    Write-Output \"$($_.BaseName) file_version=$($v.FileVersion) product=$($v.ProductVersion) bytes=$($_.Length)\"\n}\nRemove-Item -Recurse -Force $tmp -ErrorAction SilentlyContinue\nWrite-Output '--- RC ---'\n",
  "startedAt": "2026-08-25T02:54:55.240Z",
  "finishedAt": "2026-08-25T02:55:06.303Z",
  "durationMs": 11062,
  "ok": true,
  "stdout": "--- RANGED-PROBE ---\r\nTAG v0.18.0 size=2008188352 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.17.0 size=2059358691 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.16.0 size=2059177123 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.15.0 size=2058687600 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.14.0 size=2046433962 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.13.0 size=1934480722 dirs=cuda_v12,cuda_v13,vulkan cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.12.0 size=1914287790 dirs=cuda_v12,cuda_v13 cudart=lib/ollama/cuda_v12/cudart64_12.dll err=-\nTAG v0.11.0 size=1330870450 dirs= cudart=lib/ollama/cudart64_12.dll err=-\nTAG v0.10.0 size=1330121159 dirs= cudart=lib/ollama/cudart64_12.dll err=-\nTAG v0.9.0 size=1692772491 dirs=cuda_v11,cuda_v12 cudart=lib/ollama/cuda_v11/cudart64_110.dll err=-\nTAG v0.8.0 size=1692758207 dirs=cuda_v11,cuda_v12 cudart=lib/ollama/cuda_v11/cudart64_110.dll err=-\nTAG v0.7.0 size=1692744578 dirs=cuda_v11,cuda_v12 cudart=lib/ollama/cuda_v11/cudart64_110.dll err=-\nTAG v0.6.0 size=1708447205 dirs=cuda_v11,cuda_v12 cudart=lib/ollama/cuda_v11/cudart64_110.dll err=-\nTAG v0.5.0 size=1925589210 dirs=rocblas,runners cudart=lib/ollama/cudart64_110.dll err=-\nTAG v0.4.0 size=1925419149 dirs=rocblas,runners cudart=lib/ollama/cudart64_110.dll err=-\nTAG v0.3.0 size=721931143 dirs= cudart=cuda/cudart64_110.dll err=-\nNODE_RC=0\r\n--- CUDART-VERSIONS ---\r\nv0.10.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=573952\r\nv0.11.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=573952\r\nv0.12.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=573952\r\nv0.13.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582136\r\nv0.14.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582136\r\nv0.15.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582136\r\nv0.16.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582024\r\nv0.17.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582024\r\nv0.18.0.cudart file_version=6,14,11,12080 product=6,14,11,12080 bytes=582024\r\nv0.3.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\nv0.4.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\nv0.5.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\nv0.6.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\nv0.7.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\nv0.8.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505784\r\nv0.9.0.cudart file_version=6,14,11,11030 product=6,14,11,11030 bytes=505792\r\n--- RC ---\r\n",
  "stderr": null
}
