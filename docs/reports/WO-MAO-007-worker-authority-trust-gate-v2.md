# WO-MAO-007 - Worker Authority and Preventive Trust Gate v2

## Result

`COMPLETE / FAIL_CLOSED_GATE_IMPLEMENTED / PROVIDER_ADAPTERS_UNPROVEN`

Catalog registration is separated from executable worker status. Codex and Claude Code may receive at
most a bounded A8 Work Order grant covering exact actions and paths; neither receives ambient write,
commit, push, PR, merge, release, auth, secret, destructive, or canon authority. The rejected nested
Codex adapter is terminally quarantined.

The worker gate requires a matching active Work Order, attributable worker/provider/surface identity,
authority cap, action subset, and an exact match between dispatch-scope and grant paths before delegation.
It also enforces `rawCredentialInspection=false`, a recognized JSON prompt boundary, unique relative
wildcard-free paths, output redaction before persistence, cancellation support, and a separate sanitized
trust-evidence event that never copies a prompt or provider output.

The current hosted Codex session and its native team roles may coordinate the bounded Phase 1 R0/R1
proof as `PILOT_AUTHORIZED / WORKER_CANDIDATE / coordinationEligible=true`: no durable WilliamOS adapter
binds native hosted dispatch to this gate, so registry dispatch still denies them. That truthful split
does not block the current hosted coordinator from using native subagents. Claude remains
`PROVIDER_UNAVAILABLE` until an independently authenticated supported adapter exists; William is not
assigned authentication or repair.

Validation covers positive bounded Git/PR authority and negative unknown worker, disabled transport,
missing/mismatched/inactive grant, authority overflow, unsupported action, blocked paths, every required
trust field, unsafe or mismatched exact paths, recognized boundary enforcement, provider-output redaction,
independent trust evidence, and cancellation before execution.

No durable provider adapter, authenticated transport, unattended execution, or runtime activation is
claimed by this Work Order.

```text
OWNER_OPERATION_TOUCH_COUNT=0
OWNER_CREDENTIAL_TOUCH_COUNT=0
OWNER_DIAGNOSTIC_TOUCH_COUNT=0
OWNER_ROUTINE_DECISION_COUNT=0
OWNER_ROUTINE_CONTACT_COUNT=0
```
