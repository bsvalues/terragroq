# WO-MAO-006 - Agent Entrypoint Reconciliation

## Result

`COMPLETE / ONE_CANONICAL_HIERARCHY / OWNER_ONLY`

Root `AGENTS.md` now points to the active multi-agent playbook and requires bounded native Codex
subagent fan-out, non-overlapping reservations, independent assurance, agent-owned GitHub lifecycle,
and final-only owner communication. Root `CLAUDE.md` is a narrowing provider adapter rather than a
competing authority source.

The historical DevOps playbook no longer makes William the operator. Issue #357 cannot be retried or
reused, issue #358 remains dependency-blocked, and the local runtime remains disabled.

```text
OWNER_OPERATION_TOUCH_COUNT=0
OWNER_CREDENTIAL_TOUCH_COUNT=0
OWNER_DIAGNOSTIC_TOUCH_COUNT=0
OWNER_ROUTINE_DECISION_COUNT=0
OWNER_ROUTINE_CONTACT_COUNT=0
```
