# WO-RUNTIME-IDENTITY-023 - Authority Evaluator Integration

Every lease, patch, push, PR, and merge stage requires the exact repository,
registered R0/R1 work, ready dependencies, fresh base, no protected scope or
owner gate, and exact local activation. Portfolio selection cannot satisfy or
override these gates.

Result: `PASS_AUTHORITY_PRECONDITIONS`.
