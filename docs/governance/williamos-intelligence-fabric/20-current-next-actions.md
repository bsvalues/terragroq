# 20 — Current Next Actions

Completed first slice:

- IF-00 (#966) build reconciliation: current-main refresh, collision map, package-path receipt,
  commissioned-evidence reference, and independent review, with no runtime behavior change.
- IF-01 provider-neutral contracts and deterministic schema tests against the exact seams in
  `03-integration-and-migration-map.md`, with no routing activation or provider call.

Next dependency-ordered work remains separately bounded:

1. Reconcile whole-fabric review additions in this package with the settled IF-00 output.
2. Execute IF-02 as whole-fabric topology/capacity/link discovery for HERMES, AEGIS, ATLAS and opportunistic OMEN.
3. Execute IF-03 current model/runtime adoption, using `[[hermes-commissioned]]` as the canonical
   HERMES production reference rather than copying its configuration.
4. Execute IF-04 Context Fabric before automatic cross-provider/node movement.
5. Execute IF-05 benchmark/evaluation including whole-fabric and FreeToken IF-05F (#967) where safe.
6. Only then activate IF-06 shadow/local placement.

No automatic cloud burst, paid resource, hardware purchase or production routing change is authorized by these documents.
