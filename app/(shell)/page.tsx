export default function HomePage() {
  // The authenticated AppShell owns the persistent Home work surface so project,
  // thread, inspector, and execution state survive contextual route changes.
  return null
}
